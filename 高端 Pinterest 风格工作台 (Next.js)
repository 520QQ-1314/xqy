// frontend/pages/index.tsx
import Hero from '../components/Hero';
import Workspace from '../components/Workspace';
import Gallery from '../components/Gallery';

export default function LandingPage() {
  return (
    <div className="bg-white text-black selection:bg-blue-100">
      {/* 1. 极简导航 */}
      <nav className="p-6 flex justify-between items-center border-b border-gray-50">
        <div className="text-xl font-bold tracking-tighter uppercase">Agent.Vision</div>
        <div className="text-xs text-gray-400 font-mono">STATUS: HIGH-PERFORMANCE / FREE</div>
      </nav>

      {/* 2. 高端首屏 (文案驱动) */}
      <section className="py-20 px-10 text-center">
        <h1 className="text-7xl font-light tracking-tight mb-6">
          让 AI 懂你的 <span className="italic font-serif">商业美学</span>
        </h1>
        <p className="text-gray-500 max-w-2xl mx-auto mb-10 tracking-widest text-sm uppercase">
          无需注册 • 无需 API • 自动化电商视觉方案
        </p>
      </section>

      {/* 3. Agent 核心工作区 (功能整合) */}
      <section className="max-w-6xl mx-auto mb-20 bg-gray-50 rounded-[40px] p-12">
        <Workspace />
      </section>

      {/* 4. Pinterest 瀑布流 (灵感参考) */}
      <section className="px-6 pb-20">
        <div className="mb-10 pl-4 border-l-2 border-black">
          <h2 className="text-2xl font-medium">灵感库 (Explore Flow)</h2>
        </div>
        <Gallery />
      </section>

      {/* 5. 极简页脚 */}
      <footer className="p-20 text-center border-t border-gray-50 text-gray-300 text-xs tracking-widest">
        POWERED BY NANOBANANA 2 & OPEN SOURCE AGENTS
      </footer>
    </div>
  );
}
